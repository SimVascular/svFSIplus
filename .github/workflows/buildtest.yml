name: Buildtest
on: [push, pull_request]
jobs:
  build-ubuntu:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04]
      fail-fast: false
    steps:
      - uses: actions/checkout@v2
      - name: Install Ubuntu dependencies
        run: sudo apt update && sudo apt install build-essential cmake libvtk9-dev qtbase5-dev libblas4
      - name: Build svFSIplus
        run: |
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          cmake --build .